### Problem 3.26
<i>Rephrase $\lnot(A \implies B)$ using $\wedge$ or $\vee$. (Hint: Think about when $A \implies B$ is false.) Explain why $\lnot (A \implies B)$ is not an implication.</i>

| $A$ | $B$ | $\lnot (A \implies B)$ | new: $A \wedge \lnot B$ | $A \implies B$
| --- | --- | --- | --- | --- |
| T   | T   | F   | F | T
| T   | F   | T   | T | F
| F   | T   | F   | F | T
| F   | F   | F   | F | T

We propose that $\lnot (A \implies B)$ is not an implication because it is only true if $A$ does not imply $B$.



### Exercise 3.28

<i>Provide an example of a true conditional proposition whose converse is false.</i>

**True** If your pokemon is a grass type, then it is weak to fire type moves. 

**False** If your pokemon weak to fire type moves, then it is a grass type. (Ice types such as vanillux are also weak to fire). 

<div style="break-after: page;"></div>




### Exercise 3.30

<i>Provide an example of a true conditional proposition whose inverse is false.</i>

**True** If your pokemon is a ice type, then it is weak to fire type moves.

**False** If your pokemon is NOT an ice type, then it is NOT weak to fire type moves. (Grass types can be not ice types and are weak to fire). 

<div style="break-after: page;"></div>



### Exercise 3.32 

<i>Let $A$ and $B$ represent the statements from **Exercise 3.7**. Express the following
in ordinary English sentences.

1. The converse of $A \implies B$.</i>

If $6$ is a multiple of $4$, then $6$ is an even number.

<i>2. The contrapositive of $A \implies B$.</i>

If $6$ is NOT a multiple of $4$, then $6$ is NOT an even number.
 
<div style="break-after: page;"></div>



### Theorem 3.34 

<i>The implication $A \implies B$ is equivalent to its contrapositive.

The upshot of **Theorem 3.34** is that if you want to prove a conditional proposition, you
can prove its contrapositive instead. This proof strategy is called proof by contrapositive.</i>

*Proof.*

Consider the following truth table. 

| $A$ | $B$ | $A \implies B$ | $\lnot B \implies \lnot A$
| --- | --- | --- |  --- |
| T | T | T | T
| T | F | F | F
| F | T | T | T
| F | F | T | T

Now, by  **Def 2.22**, $A \implies B$ is equivalent to its contrapositive.

$\blacksquare$

<div style="break-after: page;"></div>



### Problem 3.35

<i>Consider the following statement:

Assume $n \in Z$. If $n^2$ is odd, then $n$ is an odd integer.</i>

*Proof.*

2. We will prove this by contrapositive.

8. The contrapositive of the statement “If $n^2$ is odd then $n$ is odd” is “If $n$ is an even integer,
then $n^2$ is an even integer.”

1. Thus, we assume that $n$ is an even integer.

5. By **Def 2.1**, there is an integer $k$ such that $n = 2k$.

3. Since $n = 2k$, we have that $n^2 = (2k)^2 = 4k^2$.

4. Since $k$ is an integer, $2k^2$ is also an integer by **Fact 2.3**.

9. Notice that $n^2 = 2(2k^2)$.

7. By **Definition 2.1**, $n^2$ is an even integer. 

6. Since the contrapositive is equivalent to the original statement and we have proved the contrapositive, the original statement is true.
